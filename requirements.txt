# =============================================================================
# CityGaze: UHI Research Requirements
# Compatible with: macOS (Silicon) & Linux (Colab/Cloud)
# =============================================================================

# --- Core Frameworks ---
# Note: Colab usually pre-installs these, but good to list for local dev.
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0
scikit-learn>=1.3.0    # Essential for metrics (F1, IoU)



# Image Processing

scikit-image>=0.21.0

# --- Geospatial Stack (The "Abstract" Enablers) ---
rasterio>=1.3.0        # Reading Satellite Tiffs
geopandas>=0.14.0      # Handling Shapefiles (Bangalore Wards)
shapely>=2.0.0         # Geometric operations
xarray>=2023.10.0      # Handling multi-dimensional weather data
rioxarray>=0.15.0      # Rasterio extension for xarray
folium>=0.14.0         # Interactive Maps (for visualization)
pyproj>=3.6.0          # Coordinate Reference System transformation

# Additional Geospatial Tools
pystac-client>=0.7.0
planetary-computer
meteostat>=1.6.0

# --- Image Processing ---
opencv-python-headless>=4.8.0  # Headless is safer for Cloud/Docker
Pillow>=10.0.0
albumentations>=1.3.0  # BEST for image augmentation in segmentation tasks

# --- Deep Learning Extras ---
segmentation-models-pytorch>=0.3.3  # Pre-built U-Nets/DeepLabV3+
timm>=0.9.0            # PyTorch Image Models (Backbones)
transformers>=4.35.0   # If using SegFormer or ViT based approaches

# --- MLOps & Tracking (The "Research" Enablers) ---
wandb>=0.16.0          # Weights & Biases (Replaces Tensorboard/MLflow)
hydra-core>=1.3.0      # Config management
omegaconf>=2.3.0       # Config parsing
typer>=0.9.0           # CLI interface

# --- Utilities ---
tqdm>=4.65.0           # Progress bars
python-dotenv>=1.0.0   # Loading API keys (.env)
matplotlib>=3.7.0      # Static plotting
scipy>=1.10.0

# --- Web Interface ---
gradio>=4.10.0

# Future / Optional
# torch_geometric

# Configuration
pyyaml>=6.0
